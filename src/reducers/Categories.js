const categories = ["music", "fashion", "lifestyle", "work", "travel"];

const myReducer = (state = categories, action) => {
  switch (action.types) {
    default:
      return state;
  }
};

export default myReducer;
